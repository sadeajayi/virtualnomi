{
  "name": "nomi-name-search-api",
  "version": "1.0.0",
  "description": "FastAPI backend for Nomi name search",
  "scripts": {
    "start": "uvicorn app:app --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "python": "3.11.0"
  }
}
